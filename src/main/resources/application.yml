munin:
  fixedDelay: ${SCHEDULER_TIME:30000}

spring:
  datasource:
    url: ${DB_URL:localhost}
    username: ${DB_USER:root}
    driver-class-name: org.postgresql.Driver
    password: ${DB_PASSWORD:""}
pepe_endpoint: ${PEPE_ENDPOINT:localhost:80000}
